server:
  port: 80
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka,http://127.0.0.1:7002/eureka,
ribbon:
  ReadTimeout: 5000
  connectTimeout: 5000
logging:
  level:
    com.zs.springcloud.service.MatchFeignService: debug